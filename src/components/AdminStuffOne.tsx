import Link from 'next/link'
import { trpc } from '../utils/trpc';

const AdminStuffOne: React.FC = () => {
    return (
      <>
        <button className="fill-slate-500 rounded-md">
            Create new post
        </button>
      </>
    )
  }

export default AdminStuffOne